pipeline {
	agent any
	
	stages {
		stage('Checkout') {
			steps {
				echo 'Code checking is done'
			}
		}
		stage('Build') {
			steps {
				echo 'Building in progress...'
			}
		}
		stage('Test') {
			steps {
				echo 'Testing the build...'
			}
		}
	}
}